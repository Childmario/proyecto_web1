version: '2.1'

services:
  app:
    container-name: proyecto_web1
    restart: always
    build: .
    ports:
      - 3000:3000
    links: mongo
  mongo:
    container-name: mongo
    image: mongo
    ports:
      - 27017:27017